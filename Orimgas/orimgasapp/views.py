from django.shortcuts import render

# Create your views here.
# # Create and save a PositionInstruction object
# new_position_instruction = PositionInstruction.objects.create(position=your_position_instance)

# # Now, assuming some_instruction is an existing Instruction object
# new_position_instruction.instruction.add(some_instruction)